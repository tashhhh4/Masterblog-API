[project]
name = "masterblog-api"
version = "1.0.0"
description = "The blogging API for software developers."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
]
